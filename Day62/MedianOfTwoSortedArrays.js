const solve = function(A, B) {
    const maxArray = A.length > B.length ? A : B;
    const minArray = A.length < B.length ? A : B;

    
}

const A = [1, 4, 5];
const B = [2, 3];
const res = solve(A, B);
console.log(res);